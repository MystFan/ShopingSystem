namespace ShopingRequestSystem.Domain.PublishedShopingRequests
{
    using Autofac;

    public class PublishedShopingRequestModule : Module
    {
        protected override void Load(ContainerBuilder builder)
        {
            base.Load(builder);
        }
    }
}
